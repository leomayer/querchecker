# External secrets — never committed to git.
#
# Dev:  place at querchecker/config/secrets.yml (this location)
# Prod: mount into the container at /app/config/secrets.yml
#       docker-compose.prod.yml:
#         volumes:
#           - /etc/querchecker/secrets.yml:/app/config/secrets.yml:ro
#
# Add new API keys here as the project grows. No code change required —
# just add the key, restart the backend.

querchecker:
  icecat:
    access-key: "" #
  upcitemdb:
    access-key: "" #
