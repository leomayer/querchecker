# API-Keys — nie in Git einchecken.
#
# Dev:  ablegen als querchecker/config/secrets.yml
# Prod: ins Container mounten als /app/config/secrets.yml
#       docker-compose.prod.yml:
#         volumes:
#           - /etc/querchecker/secrets.yml:/app/config/secrets.yml:ro

querchecker:
  api:
    limits:
      brave:
        api-key: "" # https://api.search.brave.com
      groq:
        api-key: "" # https://console.groq.com
      openrouter:
        api-key: "" # https://openrouter.ai
